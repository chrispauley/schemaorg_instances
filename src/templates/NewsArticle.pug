- var headObject = { title: "schema instances - LocalBusiness", description: "Uses LocalBusiness template.", keywords: "my keywords"}
- var pathObject = { currentPage: "LocalBusiness" }
doctype html
html(lang='en')
  include includes/mixins.pug
  +headSection4_Cloud(headObject)

  body(id= currentPage)
    // Navigation
    +navBar(pathObject)

    // Page Content
    .container
      // Page Heading/Breadcrumbs
      .row
        .col-lg-12
          h1.page-header
            | My SchemaOrg Instances
            small= currentPage
          ol.breadcrumb
            li
              a(href='index.html') Home
            -var typeData = locals["@type"]
            li.active= typeData

      // /.row
      .row
        .col-lg-4
          // Formatted news article goes here


      +fromGulpRow(locals)
    include includes/footer.pug
